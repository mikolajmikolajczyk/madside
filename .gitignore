# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# mads.wasm build pipeline — cloned upstream sources + intermediate artifacts.
# See justfile (`just build-mads-wasm`).
_notes/wasm-spike/build/

# Altirra wasm core fork — own git repo, tracked separately at
# github.com/mikolajmikolajczyk/AltirraSDL on branch `madside-embed`.
# See justfile (`just build-altirra-wasm`).
_notes/altirra/
