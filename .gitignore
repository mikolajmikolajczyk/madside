# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local build cache: cloned upstream sources + intermediate wasm artifacts (mads
# / cc65 pipelines) and the AltirraSDL fork checkout. Recreated on demand by the
# `just build-*` recipes — committed build INPUTS live in build-support/ instead.
# Stays on disk locally; never tracked.
_notes/

# Nix flake — direnv cache + emscripten cache + build outputs
.direnv/
.emcache/
result
result-*
.repoctx
